services:
  backend:
    build: ./example-app
    command: "php artisan serve --host=0.0.0.0 --port=8000"
    ports:
      - "8000:8000"
  web:
    build: ./front-end
    command: "npm run dev"
    ports:
     - "5173:5173"
